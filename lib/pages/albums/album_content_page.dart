import 'package:flutter/material.dart';

class AlbumContentPage extends StatelessWidget {
  const AlbumContentPage({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}
